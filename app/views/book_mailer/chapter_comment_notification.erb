The chapter, <%=h @book.title %>: <%=h @chaptel.title %>, has been updated with a new comment: 
	<%= link_to(h @chapter.name, book_chapter_url(@book, @chapter, :anchor => "comment_#{@comment.id}", :only_path => false, :host => SiteDomain ) ) %>