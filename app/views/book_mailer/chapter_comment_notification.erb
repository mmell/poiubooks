The chapter, <%=h @book.title %>: <%=h @chapter.title %>, has been updated with a new comment: 
	<%= link_to(h( @chapter.title), book_chapter_url(@book, @chapter, :anchor => "comment_#{@comment.id}", :only_path => false, :host => SiteDomain ) ) %>