Request to reset password received for <%= @user.full_name %> (<%= @user.login %>)

Visit this url to choose a new password:
 <%= reset_url(:only_path => false, :host => SiteDomain, :activation_code => @user.activation_code) %>
 