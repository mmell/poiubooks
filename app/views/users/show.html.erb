<% @page_title = "Author/Contributor: #{@user.name}" %>

<p>
  <%= @user.image_src ? image_tag(@user.image_src, :class => 'profile_image') : '' %>
  
  <%=h @user.name %>
</p>

<p>
  <b>Books:</b>
  <%= link_to("Books by #{@user.name}", user_books_path(@user)) %> (<%= @user.books.size %>)
</p>

<p>
  <b>Comments:</b>
  <%= link_to("Comments by #{@user.name}", user_comments_path(@user)) %> (<%= @user.comments.size %>)
</p>

<% if current_user_is_admin? %>
<p>
  <b>Email:</b>
  <%= mail_to @user.email %>
</p>
<% end %>
<p>
  <b>Statement:</b>
  <%= @user.description %>
</p>

<% if @user.is_admin? %>
<p>
  <b>Admin?:</b>
  <%= display_is_admin(@user) %>
</p>
<% end %>

<br clear='all' />
<% if current_user_is_admin? %>
Admin: <%= link_to 'Edit', edit_user_path(@user) %>
<% end %>
